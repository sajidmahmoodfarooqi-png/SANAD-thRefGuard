# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
# Fill in <VERSION>, <URL>, and <SHA256> for each release (see README.md in this folder —
# `wingetcreate` fills the URL + hash automatically).
PackageIdentifier: SajidMahmoodFarooqi.SANAD
PackageVersion: "<VERSION>"          # e.g. 0.2.10
InstallerType: nsis
Scope: user
InstallModes:
  - interactive
  - silent
  - silentWithProgress
UpgradeBehavior: install
Installers:
  - Architecture: x64
    InstallerUrl: "https://github.com/sajidmahmoodfarooqi-png/SANAD-thRefGuard/releases/download/v<VERSION>/SANAD.Setup.<VERSION>.exe"
    InstallerSha256: "<SHA256>"
ManifestType: installer
ManifestVersion: 1.6.0
