# SANAD the RefGuard — Core runtime + dev dependencies.
# Python 3.10+ (uses PEP 604 `X | None` type syntax).

# --- Core runtime ---
citeproc-py>=0.6            # CSL rendering engine
citeproc-py-styles>=0.1     # bundled CSL styles (APA 7th et al.)
fastapi>=0.100              # local HTTP service (add-in <-> core protocol)
uvicorn>=0.20               # ASGI server for FastAPI
websockets>=11              # uvicorn's WebSocket impl, for the /v1/events channel
pydantic>=2                 # request/response models

# --- Optional: Tier-B semantic check (R8) real backend ---
# NOT installed by default — the engine degrades to a deterministic lexical
# fallback (sanad_core/embedding.HashingEmbedding) when this is absent, and the
# scan API reports which backend actually ran. Install to get true semantic
# context-misalignment detection (pulls in torch; needs a matching Python/OS
# wheel). Uncomment, or `pip install sentence-transformers`, when available:
# sentence-transformers>=2.2

# --- Tests ---
pytest>=7
httpx>=0.24                 # required by fastapi.testclient.TestClient

# --- Legacy thesis-tooling scripts (not part of sanad_core; used by the
#     resolve_bibliography / download_papers / make_chN_apa helpers) ---
requests>=2.28
openpyxl>=3.1
python-docx>=1.0
pymupdf>=1.23
